<Window x:Class="sharp_pass.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="MainWindow" Height="300" Width="533" MinHeight="109"
        ContentRendered="OnContentRendered">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <Menu IsMainMenu="True">
            <MenuItem Header="_File">
                <MenuItem Header="_Open" />
            </MenuItem>
        </Menu>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <TreeView x:Name="passFiles" Grid.Column="0" Grid.Row="1" SelectedItemChanged="passFiles_SelectedItemChanged" />
            <DockPanel Grid.Column="1" Grid.Row="1" LastChildFill="True">
                <Button DockPanel.Dock="Bottom" Content="Copy to clipboard" Click="Button_Click"/>
                <TextBox x:Name="displayText" IsReadOnly="True" />
            </DockPanel>
            

            <GridSplitter Grid.Column="1" HorizontalAlignment="Left" Width="2" />
        </Grid>
    </Grid>
</Window>
