<Window x:Class="sharp_pass.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="MainWindow" Height="300" Width="533" MinHeight="109">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <Menu IsMainMenu="True">
            <MenuItem Header="_File">
                <MenuItem Header="_Open" />
            </MenuItem>
        </Menu>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ListView Grid.Column="0" Grid.Row="1" ItemsSource="{Binding FooBarCollection}">
                <ListView.View>
                    <GridView ColumnHeaderToolTip="foobar">
                        <GridViewColumn Header="foo" Width="Auto" DisplayMemberBinding="{Binding Foo}" />
                        <GridViewColumn Header="bar" Width="Auto" DisplayMemberBinding="{Binding Bar}" />
                    </GridView>
                </ListView.View>
            </ListView>
            <Button Grid.Column="1" Content="new row" Click="AddRow"></Button>
        </Grid>
    </Grid>
</Window>
